#include <stdio.h>
int main(void)
{
    short is_logged_in = 1;
    if (is_logged_in)
    {
        printf("User Logged In\n");
    }
    else
    {
        printf("User Logged Out\n");
    }

    return 0;
}
